package com.m2l.meta.repository;

import com.m2l.meta.entity.QuestInfo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface QuestInfoRepository extends JpaRepository<QuestInfo, Long> {
}
